import fetch from 'node-fetch';



export default async function BuyActivationNumberVoiceForwarding(api, country, operator, product, number, reuse = 0, voice = 0,) {
    try {
        // BELOM KELAR BOS
    } catch (error) {
        return { error }
    }
}